# ───────────────────────────────────────────────
# Geofence Route Generator – .gitignore
# Prevent sensitive or auto-generated files from being committed
# ───────────────────────────────────────────────

# === Node.js / TypeScript ===
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.env
.env.*
!.env.example          # Keep the template version tracked
package-lock.json
.pnpm-lock.yaml

# === Local Google Cloud credentials ===
# ⚠️ NEVER commit service account keys or OAuth secrets
*.json
!package.json          # allow dependency manifest
!tsconfig*.json        # allow config files
service-account-key.json
geofence-*.json
credentials.json
token.json

# === Temporary data ===
tmp/
temp/
logs/
*.log
*.csv
*.bak
*.tmp
*.swp
.DS_Store

# === Output files (generated by the app) ===
output/
exports/
test-output/
reports/
*.xlsx
*.ods
*.rtf

# === Test / mock data ===
/test/fixtures/*.json
/test/output/
coverage/
.nyc_output/

# === IDEs / Editors ===
.vscode/
.idea/
*.iml
*.suo
*.ntvs*
*.njsproj
*.sln

# === OS-specific cruft ===
Thumbs.db
ehthumbs.db
Desktop.ini

# === Docker / Containers ===
docker-compose.override.yml
.env.docker
*.pid
*.seed
*.tar
*.tgz

# === Python / Misc tooling (if used for scripts) ===
__pycache__/
*.py[cod]
.venv/
venv/
env/

# === Local secrets / private notes ===
notes-private.md
secrets/
.private/
